[package]
name = "bench-postgres"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-postgres = "0.7"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 2
